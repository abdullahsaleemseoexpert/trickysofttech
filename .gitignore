# Local Claude Code tooling
.claude/

# Local Netlify folder
.netlify

# OS junk
Thumbs.db
.DS_Store

# Deploy credentials (never commit these)
.ftp-config.json
*.env

# Local secrets + local tooling (GSC API key, report scripts) — never commit or deploy
.secrets/
node_modules/
